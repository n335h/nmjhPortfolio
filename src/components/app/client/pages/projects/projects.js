import Carousel from '../../components/carousel/carousel';

const projects = () => {
	return (
		<div className='ProjectTitle h-full justify-center items-center  bg-zinc-200 dark:bg-zinc-900 drop-shadow-lg'>
			<Carousel />
		</div>
	);
};

export default projects;
